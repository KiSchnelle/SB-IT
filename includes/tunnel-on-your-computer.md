!!! tip "Run the tunnel command on your own computer"
    The `ssh -N -L …` command goes into a terminal **on your laptop or desktop**, not on the cluster.
    It prints nothing and keeps running while the tunnel is open — leave that window open and press
    ++ctrl+c++ in it to close the tunnel. New to tunnels? See [SSH tunnels](/ssh_tunnels.md).
